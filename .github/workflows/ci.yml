name: CI

on:
  push:
    branches: [ "project/123140057-123140068-RailLog" ]
  pull_request:
    branches: [ "project/123140057-123140068-RailLog" ]

defaults:
  run:
    working-directory: ./123140057-123140068-RailLog-aacd548544449a45c0ec818da29e9f469ff0843b

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run tests
        run: ./gradlew :composeApp:testDebugUnitTest

      - name: Build debug APK
        run: ./gradlew :composeApp:assembleDebug